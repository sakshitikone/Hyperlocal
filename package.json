{
  "name": "hyperlocal-glitch-root",
  "version": "1.0.0",
  "description": "Root package for Glitch deployment",
  "scripts": {
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
